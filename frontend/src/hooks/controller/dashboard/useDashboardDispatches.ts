// Kept for compatibility — GraficoDisparo no longer uses this hook.
// Dispatch volume is no longer shown on the dashboard.
export function useDashboardDispatches() {
  return { dispatches: [], loading: false };
}
