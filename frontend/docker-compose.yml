services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-}
    container_name: project-charge-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8080}:80"
